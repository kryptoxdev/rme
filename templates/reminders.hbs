<!DOCTYPE html>
<html>

<head>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
		integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
	<link rel="stylesheet" href="../css/main.css">
	<meta charset="utf-8">
	<title>{{ pagetitle }}</title>
</head>

<body>
	<div class="container">
		<nav class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4">
			<div class="col-md-3 mb-2 mb-md-0"> <a class="appIcon" href="/">R.ME</a> </div>
			<ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
				<li><a href="/" class="navButtons">home</a></li>
				<li><a href="/reminders" class="navButtons active">reminders</a></li>
			</ul>
			<div class="col-md-3 text-end"></div>
		</nav>
	</div>
	<div class="container d-flex justify-content-center align-items-center">
		<div class="py-5">
			<div class="col-lg-12">
				<div class="d-flex align-items-center justify-content-center mb-3">
					<h1 class="display-6 homeText">Your Reminders</h1> 
					<a href="/reminders/add" class="btn primaryButton mx-3">+ Add Reminder</a>
				</div>
				<div class="w-100 rounded-3 shadow-lg">
					<div class="card-body">
						<div class="row">
							{{#each reminders}}
							<div class="col-sm-4 mb-4">
								<div class="card defaultText lightBackground mb-4 w-100 rounded-3 shadow-lg">
									<div class="text-center card-body">
										<h5 class="card-title">{{title}}</h5>
										<p class="card-text">{{description}}</p>
										<p class="card-text">{{date}}</p>
										<a href="/reminders/edit/{{id}}" class="btn editButton">Edit Reminder</a>
										<a href="/reminders/delete/{{id}}" class="btn deleteButton">Delete Reminder</a>
									</div>
								</div>
							</div>
							{{/each}}
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</body>

</html>
